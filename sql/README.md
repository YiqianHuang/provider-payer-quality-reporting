# SQL workstream

SQL DDL, transformations, reporting views, and validation queries begin only
after the accepted Provider raw snapshot and source profiling gate pass.

The directory structure separates:

- `ddl/`;
- `transformations/`;
- `views/`;
- `validation/`.

